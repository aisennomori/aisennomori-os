# G検定対策「知識の接続」徹底攻略ガイド
本書は、G検定（ジェネラリスト検定）の合格に向け、「見たことがある」レベルの用語知識を「問題文を見た瞬間に所属・役割・比較対象が紐解ける」実践的なレベルへと引き上げるための超整理パッケージです。

教材①.pdf、②.pdf、③.pdfの全内容を1つの連続した体系として統合し、「所属」「役割」「比較」「識別キーワード」の4軸を中心に再構成しています。

---

## ■ 作ってほしいもの 1：全体の知識マップ

教材①②③の全範囲を、網羅的な階層構造（ツリー構造）として整理しました。どこに何が属しているのか、その「所属（住所）」を常に意識して学習してください。

```text
人工知能（AI）
├ ルールベース（1次・2次ブーム）
│  ├ 1次ブーム：探索・推論
│  │  ├ 幅優先探索
│  │  ├ 深さ優先探索
│  │  ├ ミニマックス法
│  │  └ モンテカルロ法
│  ├ 2次ブーム：知識の表現
│  │  ├ 意味ネットワーク
│  │  ├ オントロジー（ヘビーウェイト / ライトウェイト）
│  │  └ エキスパートシステム（MYCIN, DENDRAL, CASNET）
│  └ ルールベースの限界と問題
│     ├ トイ・プロブレム
│     ├ 知識獲得のボトルネック
│     ├ フレーム問題
│     ├ シンボルグラウンディング問題
│     ├ モラベックのパラドックス
│     └ 身体性
│
├ 機械学習（2次・3次ブーム）
│  ├ 機械学習の主要分野（前提知識）
│  │  ├ 半教師あり学習（アノテーション）
│  │  └ 汎化性能とエラー要因（過学習, 未学習, バイアス, バリアンス, データリーケージ）
│  │
│  ├ 教師あり学習
│  │  ├ 分類（ロジスティック回帰, 決定木, KNN, ナイーブベイズ, SVM）
│  │  ├ 回帰（線形回帰）
│  │  └ アンサンブル学習（バギング, ブースティング）
│  │     ├ バギング：ブートストラップ法、ランダムフォレスト
│  │     └ ブースティング：勾配ブースティング、AdaBoost【資料外補足】
│  │
│  ├ 教師なし学習
│  │  ├ クラスタリング
│  │  │  ├ 階層型（Ward法）
│  │  │  └ 非階層型（k-means）
│  │  └ 次元削減・可視化
│  │     ├ 主成分分析（PCA）
│  │     └ t-SNE
│  │
│  ├ 精度評価と最適化
│  │  ├ 評価データ（訓練データ, テストデータ, 検証データ【資料外補足】）
│  │  ├ 評価手法（ホールドアウト法, 交差検証法）
│  │  ├ 評価指標（混同行列：Accuracy, Precision, Recall, F値 / 回帰誤差：MAE, MSE, RMSE）
│  │  └ ハイパーパラメータ（グリッドサーチ, ランダムサーチ）
│  │
│  └ データ加工（前処理）
│     ├ 特徴量、ラベルエンコーディング、One-Hotエンコーディング
│     ├ 不均衡データ対策（ダウンサンプリング, アンダーサンプリング, オーバーサンプリング, SMOTE）
│     └ データの操作（標準化, 正規化, 白色化, 平滑化）
│
└ ディープラーニング（3次ブーム）
   ├ 学習の基本
   │  ├ パーセプトロン（単純, 多層）
   │  ├ 活性化関数（シグモイド, Tanh, ReLU, ソフトマックス）
   │  ├ 誤差逆伝播法
   │  └ 最適化手法（SGD, モーメンタム, AdaGrad, RMSprop, Adam）
   ├ 学習の工夫と軽量化
   │  ├ 過学習抑制（早期終了, ドロップアウト, バッチ正規化）
   │  ├ モデル軽量化（プルーニング, 蒸留, 量子化）
   │  └ 応用学習（データ拡張, 転移学習, ファインチューニング）
   │
   ├ 代表的ネットワークと発展手法
   │  ├ CNN（畳み込み, プーリング, パディング, ストライド, スキップ結合 / ResNet, AlexNet, GoogLeNet）
   │  ├ RNN（BPTT, 長期依存 / LSTM, GRU）
   │  ├ オートエンコーダ（エンコード, デコード, 積層オートエンコーダ, 稀少オートエンコーダ）
   │  └ 生成モデル（GAN, DCGAN, VAE, Pix2Pix, CycleGAN）
   │
   ├ 応用分野（物体認識・自然言語処理・音声）
   │  ├ 説明可能性（XAI：LIME, Grad-CAM, SHAP）
   │  ├ 物体検出・セグメンテーション（YOLO, SSD, FCN, U-Net, Mask R-CNN）
   │  ├ 自然言語処理（形態素解析, MeCab, Word2Vec, Transformer, Self-Attention, BERT, GPT）
   │  └ 音声認識・生成（サンプリング定理, HMM, WaveNet）
   │
   ├ 深層強化学習（DQN, RLHF, AlphaGo）
   │  └ ※詳細は「強化学習専用マップ」を参照
   │
   └ 社会実装・法規制・倫理
      ├ プロジェクト手法（準委任, 請負, NDA, PoC, CRISP-DM, MLOps）
      ├ システム技術（IoT, RPA, BI, PaaS, IaaS, SaaS, エッジAI, GPU）
      ├ 国内法規制（個人情報保護法, 著作権法30条の4, 特許法, 不正競争防止法）
      └ 倫理ガイドライン（XAI, 公平性, 説明責任, GDPR, EU AI規制法）
```

---

## ■ 作ってほしいもの 2：混同しやすい比較表

試験で最も失点しやすい「似た者同士」の概念を、4つの軸（所属・役割・比較・識別キーワード）で整理しました。

### 1. 教師あり vs 教師なし vs 強化学習
*   **所属**：機械学習の3大主要分野 [3]
*   **役割**：
    *   **教師あり**：入力と正解（教師ラベル）の関係性を学習し、未知のデータの予測・分類を行う [3]
    *   **教師なし**：正解ラベルのないデータから、構造やパターン（グループ分けや次元削減）を見出す [3]
    *   **強化学習**：エージェントが環境と相互作用しながら、得られる「報酬（の期待値）が最大」となるような行動指針を学習する [3, 10, 14]
*   **比較**：
    *   教師ありは「正解（ラベル）」がある [3]。
    *   教師なしは「正解」がない [3]。
    *   強化学習は即時の正解ではなく、試行錯誤を通じて将来得られる「累積報酬の最大化」を目指す [10, 14]。
*   **識別キーワード**：
    *   教師あり：正解ラベル、分類、回帰 [3]
    *   教師なし：データの構造・特徴、クラスタリング、次元削減 [3]
    *   強化学習：エージェント、環境、行動、報酬（の最大化） [3, 10, 14]

### 2. 分類 vs 回帰
*   **所属**：教師あり学習 [3]
*   **役割**：
    *   **分類**：データをあらかじめ決められたカテゴリ（クラス）に割り当てる [3]。
    *   **回帰**：数値を予測する（連続値を予測する） [3]。
*   **比較**：
    *   予測する対象が出力（離散値・カテゴリ）か、連続値（数値）かという違い [3]。
*   **識別キーワード**：
    *   分類：カテゴリに割り当てる、クラス、ロジスティック回帰（2分類） [3]
    *   回帰：数値を予測する、連続値、線形回帰 [3]

### 3. Lasso vs Ridge
*   **所属**：教師あり学習における「正則化（過学習抑制手法）」 [3]
*   **役割**：
    *   **Lasso**（L1正則化）：不要なパラメータ（重み）を完全に「0」にし、特徴量を選択する [3]。
    *   **Ridge**（L2正則化）：パラメータ（重み）の値を全体的に小さく抑え、過学習を防ぐ [3]。
*   **比較**：
    *   Lassoは「重みを完全に0にできる（スパースなモデル）」ため特徴量選択の効果がある [3]。
    *   Ridgeは「重みを0にせず、全体的に縮小する（滑らかにする）」 [3]。
*   **識別キーワード**：
    *   Lasso：L1、絶対値の和、パラメータ（重み）を0にする、スパース [3]
    *   Ridge：L2、二乗の和、全体を小さく抑える [3]

### 4. L1正則化 vs L2正則化
*   **所属**：過学習を防ぐためのペナルティ項（正則化）の算出方法 [3]
*   **役割**：
    *   **L1正則化**：パラメータの絶対値の和をペナルティ（罰則項）として損失関数に加える [3]。
    *   **L2正則化**：パラメータの二乗和をペナルティとして損失関数に加える [3]。
*   **比較**：
    *   数式に「絶対値」を用いるか、「二乗」を用いるかの違い。L1はLasso、L2はRidgeに対応する [3]。
*   **識別キーワード**：
    *   L1正則化：絶対値の和、Lasso、パラメータを0にする [3]
    *   L2正則化：二乗の和、Ridge、パラメータを小さく均一に抑える [3]

### 5. Bagging vs Boosting
*   **所属**：機械学習の「アンサンブル学習（複数のモデルの組み合わせ）」 [3]
*   **役割**：
    *   **Bagging**：学習データをランダムに分割（ブートストラップ法）し、複数のモデルを個別に同時学習させて平均や多数決をとる [3]。
    *   **Boosting**：1つのモデルを学習させた後、そのモデルが間違えたデータに重み（重要度）をつけ、次のモデルがそれを補うように逐次的に学習を進める [3]。
*   **比較**：
    *   Baggingは「並列（並行）」に独立して学習するため、バリアンス（ばらつき）を減らす効果がある [3]。
    *   Boostingは「直列（逐次的）」に前モデルの誤りを補いながら学習するため、バイアス（偏り）を減らす効果がある [3]。
*   **識別キーワード**：
    *   Bagging：並列、ブートストラップ、独立、ランダムフォレスト [3]
    *   Boosting：直列、逐次的、前の誤りを補う、重み、勾配ブースティング [3]

### 6. k-means vs Ward法
*   **所属**：教師なし学習における「クラスタリング」 [3]
*   **役割**：
    *   **k-means**：データをあらかじめ指定されたK個のクラスタに分ける（非階層型） [3]。
    *   **Ward法**：データを似た者同士でボトムアップに結合し、樹状図を作る（階層型） [3]。
*   **比較**：
    *   k-meansは「非階層型」で、あらかじめクラスタ数Kの指定が必要。重心の更新を繰り返す [3]。
    *   Ward法は「階層型」で、クラスタ内の分散（平方和）の増加が最も小さくなるように結合していくため、クラスタ数の事前決定は不要 [3]。
*   **識別キーワード**：
    *   k-means：非階層型、K個、重心を更新 [3]
    *   Ward法：階層型、デンドログラム（樹状図）、分散の増加を最小化 [3]

### 7. 特徴量選択 vs 特徴抽出
*   **所属**：機械学習の前処理・次元削減 [3]
*   **役割**：
    *   **特徴量選択**：元のデータ（特徴量）の中から、予測に役立つ「有効な列だけを選び出す」（不要な列は捨てる） [3]。
    *   **特徴抽出**：元のデータを数式などによって別の空間へ写像（変換）し、新しい特徴量を「合成・生成する」 [3]。
*   **比較**：
    *   特徴量選択は元の変数の意味を保ったまま絞り込む。
    *   特徴抽出は元の変数を組み合わせて新しい変数を作るため、次元は下がるが解釈性が変わる（PCAなど） [3]。
*   **識別キーワード**：
    *   特徴量選択：必要なものを選ぶ、不要なものを捨てる、Lasso（L1） [3]
    *   特徴抽出：新たな変数を生成、次元削減、PCA、t-SNE [3]
*   【教材記載】PCAは「相関のある多くの変数を相関のない少数の変数に要約（第一主成分など）する次元削減手法」として紹介されている [3]。特徴量選択と特徴抽出の対比は【資料外補足】として補い、G検定の選択肢対策として整理。

### 8. 標準化 vs 正規化 vs 白色化
*   **所属**：データの操作（前処理） [3, 11]
*   **役割**：
    *   **標準化**：平均値を0、標準偏差（分散）を1にする変換 [3, 11]。
    *   **正規化**：データの値を一定範囲（例：0から1の間）に収める変換 [3, 11]。
    *   **白色化**：データ間の相関をなくし、かつ分散を1（正規化）にする変換 [3, 11]。
*   **比較**：
    *   標準化は分布のスケール（平均とばらつき）を統一する [3]。
    *   正規化は最大最小を用いて範囲を固定する（外れ値に弱い） [3, 11]。
    *   白色化はさらに変数同士の「共分散（相関）」を排除して無相関化する [3, 11]。
*   **識別キーワード**：
    *   標準化：平均0、標準偏差1、ばらつきを揃える [3, 11]
    *   正規化：一定範囲（0〜1）、外れ値の影響を受けやすい [3, 11]
    *   白色化：相関をなくす、分散の正規化 [3, 11]

### 9. 訓練データ vs 検証データ vs テストデータ
*   **所属**：モデルの学習と精度評価 [3]
*   **役割**：
    *   **訓練データ**（学習データ）：モデルのパラメータ（重みなど）を学習（調整）するために用いる [3]。
    *   **検証データ**（バリデーション）：【資料外補足】学習中に、過学習の検知やハイパーパラメータ（エポック数など）の調整を行うために用いる。
    *   **テストデータ**：最終的なモデルの「汎化性能（未知のデータへの対応力）」を測定・検証するために用いる [3]。
*   **比較**：
    *   訓練データは「パラメータの学習用」、検証データは「ハイパーパラメータのチューニング用」、テストデータは「最終評価用（学習には一切関与させない）」 [3]。
*   **識別キーワード**：
    *   訓練データ：モデルを学習、学習データ [3]
    *   検証データ：【資料外補足】ハイパーパラメータ調整、過学習の監視
    *   テストデータ：汎化性能を測る、一度だけ使用 [3]

### 10. ホールドアウト vs 交差検証
*   **所属**：モデルの評価手法 [3]
*   **役割**：
    *   **ホールドアウト法**：データを「訓練データ」と「テストデータ」の2つに一度だけ分割して評価する [3]。
    *   **交差検証（K-分割交差検証）**：データをK個に分割し、そのうち1つをテスト用、残りを訓練用とする評価を、テストデータを入れ替えながらK回繰り返して平均精度を求める [3]。
*   **比較**：
    *   ホールドアウトは簡単だが、データの分割方法によって精度に偏り（過学習やアンダーフィッティングの誤認）が生じる可能性がある [3]。
    *   交差検証は計算コストが高いが、データが少ない場合でも偏りの少ない信頼性の高い評価ができる [3]。
*   **識別キーワード**：
    *   ホールドアウト：一度だけ分割、簡単 [3]
    *   交差検証：K分割、テストデータを入れ替え、データ量が少ないときに有利 [3]

### 11. 過学習 vs 未学習
*   **所属**：機械学習の学習モデルの状態 [3]
*   **役割・現象**：
    *   **過学習**（オーバーフィッティング）：訓練データに適合しすぎて、未知のテストデータに対する予測精度（汎化性能）が低下した状態 [3]。
    *   **未学習**（アンダーフィッティング）：モデルの表現力が不足しており、訓練データすら十分に学習できていない状態 [3]。
*   **比較**：
    *   過学習は「訓練データには強いがテストデータに弱い」 [3]。
    *   未学習は「訓練データにもテストデータにも弱い」 [3]。
*   **識別キーワード**：
    *   過学習：表現力過剰、特徴量が多すぎる、データ不足、汎化性能の喪失 [3]
    *   未学習：表現力不足、訓練データすら学習できていない [3]

### 12. Precision（適合率） vs Recall（再現率）
*   **所属**：分類タスクにおける「精度評価指標（混同行列）」 [3]
*   **役割・数式**：
    *   **Precision**：モデルが「陽性」と予測したもののうち、本当に陽性だった割合。式：$TP / (TP + FP)$ [3]。
    *   **Recall**：実際に「陽性」であるデータのうち、モデルが漏らさず「陽性」と予測できた割合。式：$TP / (TP + FN)$ [3]。
*   **比較**：
    *   Precisionは「間違い（無駄な陽性判定＝偽陽性）」を減らしたいときに重視する（スパムメール判定など）。
    *   Recallは「見落とし（陰性判定の誤り＝偽陰性）」を減らしたいときに重視する（がんの病気検診など）。
*   **識別キーワード**：
    *   Precision：陽性判定の正しさ、適合率、分母が予測陽性 [3]
    *   Recall：見落としを防ぐ、再現率、異常データの検出率、分母が実物陽性 [3]

### 13. MAE vs MSE vs RMSE
*   **所属**：回帰タスクにおける「精度評価指標」 [3]
*   **役割・数式**：
    *   **MAE**（平均絶対誤差）：予測値と実測値の差の「絶対値」の平均 [3]。
    *   **MSE**（平均二乗誤差）：予測値と実測値の差の「二乗」の平均 [3]。
    *   **RMSE**（平方根平均二乗誤差）：MSEの「平方根」 [3]。
*   **比較**：
    *   MAEは外れ値（極端なエラー）の影響を受けにくい（堅牢） [3]。
    *   MSEは二乗するため大きなエラーに対して極めて厳しいペナルティを与える [3]。
    *   RMSEはMSEにルートをかけることで、評価の単位を元のデータの実数値と揃えることができる [3]。
*   **識別キーワード**：
    *   MAE：絶対値、外れ値の影響を受けにくい [3]
    *   MSE：二乗、誤差に敏感（厳しいペナルティ） [3]
    *   RMSE：平方根、元のデータと単位を合わせる [3]

### 14. AIC vs BIC
*   **所属**：統計的モデル選択の「情報量基準」【資料外補足】
*   **役割・数式**：
    *   **AIC**（赤池情報量基準）：モデルの適合度（最大尤度）とパラメータ数のバランスを評価する指標。式：$AIC = -2 \ln(L) + 2k$（$L$は最大尤度、$k$はパラメータ数）。
    *   **BIC**（ベイズ情報量基準）：AICと同様の目的だが、サンプルサイズを考慮した指標。式：$BIC = -2 \ln(L) + k \ln(n)$（$n$はサンプルサイズ）。
*   **比較**：
    *   AICはサンプルサイズに依存しない。
    *   BICはサンプルサイズ $n$ が大きくなるにつれてパラメータ数の増加（モデルの複雑化）に対してAICよりも厳しいペナルティを課す。
*   **識別キーワード**：
    *   AIC：赤池情報量基準、尤度とパラメータ数、サンプルサイズ非依存
    *   BIC：ベイズ情報量基準、サンプルサイズによるペナルティ
*   【教材記載】第3章の問題に出てくる範囲として挙げられていますが、教材内には直接の解説テキストが存在しないため、一般的な定義を【資料外補足】として整理。

### 15. 価値関数 vs 方策
*   **所属**：強化学習の基本構成要素 [10, 15]
*   **役割**：
    *   **方策**：ある状態において、どのような行動をとるかを決める「行動ルール（確率分布や決定規則）」 [10, 15]。
    *   **価値（価値関数）**：ある状態にいる、またはある状態で特定の行動をとったとき、将来にわたって得られる「累積報酬の期待値」 [10, 15]。
*   **比較**：
    *   方策は「行動（Action）の選択指針」そのもの [10, 15]。
    *   価値関数は「状態（State）や行動の良さ」を測るスケール（評価） [10, 15]。
*   **識別キーワード**：
    *   方策：行動ルール、どのような行動をとるか [10, 15]
    *   価値（価値関数）：将来の累積報酬の期待値 [10, 15]

### 16. Q値 vs 状態価値
*   **所属**：強化学習の価値関数 [10, 14]
*   **役割**：
    *   **Q値**（行動価値 $Q(s, a)$）：ある状態 $s$ で特定の「行動 $a$」を選択し、その後は方策に従った場合の将来累積報酬の期待値 [10, 14]。
    *   **状態価値**（$V(s)$）：特定の「状態 $s$」にいること自体の良さを表し、そこから方策に従った場合の将来累積報酬の期待値 [10, 14]。
*   **比較**：
    *   状態価値 $V(s)$ は「状態」だけを引数にする [10]。
    *   Q値 $Q(s, a)$ は「状態」と「行動」の両方を引数にする [10]。行動の意思決定に直接使えるのはQ値である。
*   **識別キーワード**：
    *   Q値（行動価値）：状態と行動、Qテーブル、Q学習 [10, 14]
    *   状態価値：状態そのものの良さ、V [10, 14]

### 17. REINFORCE vs Actor-Critic
*   **所属**：強化学習の方策勾配ベースの手法 [14]
*   **役割**：
    *   **REINFORCE**：【資料外補足】モンテカルロ法を用いてエピソード全体の累積報酬が確定してから方策パラメータを更新する、最も基本的な方策勾配法。
    *   **Actor-Critic**：方策に従って行動を選択する「Actor」と、その行動を評価して価値関数を更新する「Critic」の2つを同時に学習させる手法 [14]。
*   **比較**：
    *   REINFORCEはエピソードが最後まで完了しないと更新できない（モンテカルロ型、分散が大きい）。
    *   Actor-CriticはCritic（価値評価器）を導入することで、毎ステップ（あるいは短いステップ）でTD誤差を用いて逐次更新が可能（時間差型、分散が小さい） [14]。
*   **識別キーワード**：
    *   REINFORCE：【資料外補足】エピソード終了まで待つ、モンテカルロ方策勾配
    *   Actor-Critic：Actor（行動器）＋ Critic（評価器）、逐次更新、TD誤差 [14]

---

## ■ 作ってほしいもの 3：瞬殺キーワード

問題文に以下のトリガーワード（キーワード）が出現した際、正解の選択肢として真っ先に疑うべき1対1の対応リストです。試験中の時間短縮に直結します。

| 問題文のトリガーキーワード | 疑うべき正解単語 |
| :--- | :--- |
| **最大マージン**、境界線決定 [5] | **SVM（サポートベクトルマシン）** |
| **非線形・高次元写像**、無限次元の超平面 [5] | **カーネルトリック** |
| **重心の更新**を繰り返す、非階層 [5, 6] | **k-means（K平均法）** |
| **階層型**クラスタリング、デンドログラム、樹状図 [5] | **Ward法** |
| **L1**、パラメータ（重み）を**完全に0**にする、スパース [3, 4] | **Lasso** |
| **L2**、パラメータを小さく抑える（0にはしない） [3, 4] | **Ridge** |
| **並列**、個別学習、多数決、ブートストラップ [4] | **Bagging（バギング）** |
| **直列**、逐次的、**前の誤りを次が補う** [4, 5] | **Boosting（ブースティング）** |
| **将来の累積報酬**の期待値 [10, 15] | **価値（価値関数）** |
| **Actor（行動選択） + Critic（価値評価）** [14] | **方策 + 価値関数（Actor-Critic）** |
| **データ間の相関をなくし**、分散を正規化 [3, 11] | **白色化** |
| 陽性予測における正解率、**偽陽性を減らしたい** [3, 5] | **適合率（Precision）** |
| 実際の陽性に対する検出率、**見落としを防ぎたい** [3, 5] | **再現率（Recall）** |
| **スキップ結合**、跳び越え、150層を超える超深層 [9] | **ResNet** |
| 勾配がゼロに近い平坦な領域、**学習が停滞** [8] | **プラトー** |
| **内部共変量シフト**の緩和、レイヤー間の分布調整 [8] | **バッチ正規化** |
| 隠れ層がデコード、**入力と同じデータを出力** [10] | **オートエンコーダ** |
| **人間からのフィードバック**、3ステップ、ChatGPT [14] | **RLHF（人間からのフィードバックによる強化学習）** |
| 著作物の利用、**AI学習における権利制限** [16] | **著作権法第30条の4** |
| 物理環境のパラメータを**ランダムに変動**させシミュレーション [14] | **ドメインランダマイゼーション** |

---

## ■ 作ってほしいもの 4：仲間外れ問題

「所属」を意識し、カテゴリの違いを瞬時に判別するための「仲間外れ問題」です。すべて4択形式で、詳細な解説を付記しています。

### 【第1問：学習分類の仲間外れ】
次の手法のうち、**所属カテゴリ（学習の種類）が異なるもの**を1つ選べ。
1.  k-means
2.  PCA（主成分分析）
3.  Ward法
4.  SVM（サポートベクトルマシン）

*   **正解**：4. SVM
*   **各選択肢の所属**：
    1.  k-means：**教師なし学習**（クラスタリング） [3]
    2.  PCA：**教師なし学習**（次元削減） [3]
    3.  Ward法：**教師なし学習**（クラスタリング） [3]
    4.  SVM：**教師あり学習**（分類） [3]
*   **なぜ仲間外れなのか**：1、2、3はすべて「正解ラベルを与えずに、データ自体の特徴や構造を学習する」**教師なし学習**に所属しますが、4のSVMだけは「正解ラベルを用いてクラスの境界線を学習する」**教師あり学習**に属するため、仲間外れとなります [3]。

---

### 【第2問：最適化手法の仲間外れ】
次の用語のうち、**所属カテゴリが異なるもの**を1つ選べ。
1.  AdaGrad
2.  RMSprop
3.  Lasso
4.  Adam

*   **正解**：3. Lasso
*   **各選択肢の所属**：
    1.  AdaGrad：**ニューラルネットワークの最適化手法（学習率調整アルゴリズム）** [8]
    2.  RMSprop：**ニューラルネットワークの最適化手法（学習率調整アルゴリズム）** [8]
    3.  Lasso：**過学習を防ぐための正則化（L1ペナルティ）** [3]
    4.  Adam：**ニューラルネットワークの最適化手法（学習率調整アルゴリズム）** [8]
*   **なぜ仲間外れなのか**：1、2、4はすべて「ディープラーニングにおいて勾配を用いて重みを更新する際の、学習率を適応的に調整する手法」ですが、3のLassoだけは「モデルの損失関数に重みの絶対値ペナルティを課して過学習を防ぐ手法（正則化）」であるため、仲間外れとなります [3, 8]。

---

### 【第3問：強化学習アプローチの仲間外れ】
次の強化学習に関連する手法のうち、**価値ベース（Value-based）ではないアプローチ**を1つ選べ。
1.  Q学習（Q-Learning）
2.  SARSA
3.  DQN（Deep Q-Network）
4.  REINFORCE

*   **正解**：4. REINFORCE
*   **各選択肢の所属**：
    1.  Q学習：**価値ベースアプローチ**（状態行動価値Q値を更新） [10, 14]
    2.  SARSA：**価値ベースアプローチ**（状態行動価値Q値を更新） [10, 14]
    3.  DQN：**価値ベースアプローチ**（ニューラルネットワークでQ値を近似） [10, 14]
    4.  REINFORCE：**方策ベース（方策勾配法）アプローチ**【資料外補足】
*   **なぜ仲間外れなのか**：1、2、3はすべて「状態や行動の『価値（Q値）』をモデル化・更新して最適な意思決定を行う」**価値ベース**の手法ですが、4のREINFORCEだけは「価値関数を介さず、方策のパラメータを直接勾配法によって更新する」**方策ベース**（方策勾配法）の手法であるため、仲間外れとなります [10, 14]。

---

### 【第4問：ディープラーニング軽量化の仲間外れ】
次のディープラーニングの技術のうち、**「モデルの軽量化（エッジデバイスなどへの実装工夫）」に属さないもの**を1つ選べ。
1.  プルーニング
2.  蒸留
3.  バッチ正規化
4.  量子化

*   **正解**：3. バッチ正規化
*   **各選択肢の所属**：
    1.  プルーニング：**モデルの軽量化手法**（寄与の低いノードを切る） [8]
    2.  蒸留：**モデルの軽量化手法**（親モデルの知識を小モデルに継承） [8]
    3.  バッチ正規化：**学習の最適化・過学習抑制手法**（レイヤー間データの正規化） [8]
    4.  量子化：**モデルの軽量化手法**（ビット精度を下げて容量削減） [8]
*   **なぜ仲間外れなのか**：1、2、4はいずれも「学習済みニューラルネットワークのサイズや計算負荷を削減し、軽量化するための手法」ですが、3のバッチ正規化は「学習自体の進行速度を上げたり、勾配消失を防いだり、内部共変量シフトを抑えて学習を安定化させるためのアプローチ（軽量化ではない）」であるため、仲間外れとなります [8]。

---

## ■ 作ってほしいもの 5：強化学習の専用マップ

最も苦手とされている強化学習について、「上位概念 → 下位手法」の階層関係を厳密に整理しました。教材内の明記部分 [10, 14, 15] と【資料外補足】を完全に分けて構成しています。

### 【強化学習の全体階層マップ】

```text
強化学習（基本設定：マルコフ決定過程（MDP）[10, 14]）
│  ├ 状態（State / s）[10, 14]
│  ├ 行動（Action / a）[10, 14]
│  ├ 報酬（Reward / r）[10, 14]
│  └ マルコフ性（未来は現在にのみ依存）[10, 14]
│
├ 1. 探索と利用のトレードオフ（多腕バンディット問題への解決手法）
│  ├ ε-greedy法：一定確率εでランダム、1-εで最大価値を選択 [10, 14]
│  ├ greedy法：常に評価値最大のみを選択 [10]
│  └ UCB（Upper Confidence Bound）アルゴリズム 【資料外補足】
│
├ 2. 価値評価の基準
│  ├ 状態価値関数 V(s)：ある状態そのものの良さ（将来期待報酬） [10, 14]
│  └ 行動価値関数 Q(s, a)（Q値）：ある状態で特定の行動をとる価値 [10, 14]
│
├ 3. アルゴリズムの分類（価値ベース vs 方策ベース vs モデルベース）
│  │
│  ├ A. 価値ベース（Value-based / Q値やV値をモデル化・更新する）
│  │  ├ モンテカルロ法：エピソード終了後に一気に価値を更新 [10]
│  │  ├ 1ステップ更新型（時間差学習 / TD学習）
│  │  │  ├ Q学習（Q-Learning）：最適行動の推定値でQ値を更新 [10, 14]
│  │  │  └ SARSA：実際にエージェントが取った次の行動の評価で更新 [10, 14]
│  │  └ 深層価値ベース（DQN系）
│  │     ├ DQN（Deep Q-Network）：NNでQ値を近似 [10, 14]
│  │     │  ├ 拡張工夫1：Experience Replay（経験再生）[10, 14]
│  │     │  └ 拡張工夫2：ターゲットネットワーク（TD誤差安定化） [14]
│  │     ├ ダブルDQN：過大評価を防ぐ拡張 [14]
│  │     └ RAINBOW：DQNの拡張手法（ダブルDQN、デュエリング、ノイジー等）の全部載せ [14]
│  │
│  ├ B. 方策ベース（Policy-based / 行動決定ルールの方策を直接学習）
│  │  └ 方策勾配法（Policy Gradient Methods）[15]
│  │     └ REINFORCE：モンテカルロ型の方策勾配法 【資料外補足】
│  │
│  ├ C. 価値＋方策ハイブリッド（Actor-Critic系）
│  │  ├ Actor-Critic：行動器（Actor）と評価器（Critic）を同時学習 [14]
│  │  ├ A3C（Asynchronous Advantage Actor-Critic）：並列・非同期化 [14]
│  │  ├ A2C（Advantage Actor-Critic）：同期化 【資料外補足】
│  │  ├ TRPO（Trust Region Policy Optimization）：信頼領域 【資料外補足】
│  │  ├ PPO（Proximal Policy Optimization）：クリッピング（Clipping）による近似 【資料外補足】
│  │  ├ DDPG（Deep Deterministic Policy Gradient）：連続行動空間に対応 【資料外補足】
│  │  └ SAC（Soft Actor-Critic）：最大エントロピー理論に基づく安定学習 【資料外補足】
│  │
│  └ D. 環境モデルの有無による分類
│     ├ モデルフリー（環境パラメータ推定なし。Q学習、SARSAなど直接方策や価値を学習）[15]
│     └ モデルベース（環境モデルを推定・シミュレートして学習）[15]
│
└ 4. 特殊・応用手法
   ├ sim2real：シミュレーションから実世界への適用 [14]
   ├ ドメインランダマイゼーション：物理パラメータのランダム化 [14]
   ├ オンライン強化学習：環境と直接作用 [14]
   ├ オフライン強化学習：過去データのみを使用 [14]
   └ RLHF（人間からのフィードバックによる強化学習）：ChatGPT等で利用 [14]
```

### 【資料外補足】強化学習の重要語・詳細
*   **UCB（Upper Confidence Bound）**：多腕バンディット問題において、「これまでの平均報酬（利用）」に「まだ試していない不確実さ（探索）」を加え、探索の優先度を数式的に決める手法。
*   **A2C（Advantage Actor-Critic）**：A3Cの非同期（Asynchronous）部分を「同期（Synchronous）」に変更した手法。GPUでの学習効率が良く、並列エージェントの更新を同期して行う。
*   **TRPO（Trust Region Policy Optimization）**：方策の更新幅が大きすぎて学習が崩壊するのを防ぐため、更新前後の「方策の分布の差（KLダイバージェンス）」に制約を設けた手法（信頼領域）。計算コストが高い。
*   **PPO（Proximal Policy Optimization）**：TRPOの複雑な計算を、方策の変化率に制限をかける「Clipping（クリッピング）」という単純な数式で近似した、非常に実用的で標準的な手法。
*   **DDPG（Deep Deterministic Policy Gradient）**：DQNは離散的な行動（例：右、左）にしか使えませんが、これを「ロボットの関節角度」などの「連続的な行動空間」へ適用できるようにした決定論的方策勾配法。
*   **SAC（Soft Actor-Critic）**：DDPG同様に連続行動空間に対応し、「報酬の最大化」に加えて「行動の多様性（エントロピー）」も最大化することで、非常に安定した効率的な学習を実現する手法。

---

## ■ 作ってほしいもの 6：第3章の復習用まとめ（試験直前5分前確認シート）

試験開始の5分前にスマートフォンの画面を上から下へスクロールするだけで、迷いやすい最頻出ポイントを脳に再インプットするための超高密度サマリーです。

### 🚨 1. 教師あり vs 教師なし（所属の境界線）
*   **教師あり**：線形回帰、ロジスティック回帰、決定木、ランダムフォレスト、SVM、KNN、ナイーブベイズ [3]
*   **教師なし**：k-means、Ward法（階層クラスタリング）、PCA（主成分分析）、t-SNE [3]
*   **注意（KNNはどっち？）**：KNN（K近傍法）は教師あり学習（分類・回帰）です。「教師なしのクラスタリングに似ている」と混同を誘う問題が頻出します [3, 5]。

### 🚨 2. クラスタリングと次元削減（教師なしの二頭政治）
*   **クラスタリング**：
    *   **階層型（Ward法）**：距離の近いデータから順に結合。**デンドログラム（樹状図）**を作る [5]。
    *   **非階層型（k-means）**：クラスタ数Kを事前設定。**「重心の更新」**を繰り返す [5]。
*   **次元削減**：
    *   **PCA（主成分分析）**：線形な次元削減。相関のある変数群を、相関のない少数の主成分に合成する [5]。
    *   **t-SNE**：非線形な次元削減。高次元の局所的な関係性を保ったまま2次元や3次元に圧縮して可視化する [5]。

### 🚨 3. SVM（サポートベクトルマシン）
*   **役割**：2クラスの境界線を引く強力な分類器 [3]。
*   **仕組み**：境界線から最も近いデータ（**サポートベクトル**）との距離（**マージン**）を**最大化**する [5]。
*   **拡張**：線形分離できないデータを、高次元に写像して境界線を引く技術を**カーネルトリック**と呼ぶ [5]。

### 🚨 4. 正則化：Lasso vs Ridge（数式と効果）
*   **Lasso（L1）**：絶対値ペナルティ。パラメータ（重み）を**完全に0**にできる（特徴量選択に使える） [3]。
*   **Ridge（L2）**：二乗ペナルティ。パラメータを0にはせず、全体的に**滑らかに縮小**させる [3]。

### 5. アンサンブル学習：Bagging vs Boosting
*   **Bagging**：データを**並列**（独立）に学習。多数決や平均を取る。過学習（バリアンス）を防ぐ。例：ランダムフォレスト [3]。
*   **Boosting**：データを**直列**（逐次）に学習。**「前の誤りを次が補う」**。表現力不足（バイアス）を埋める。例：勾配ブースティング [3, 5]。

### 6. データ前処理：標準化 vs 正規化 vs 白色化
*   **標準化**：平均を0、標準偏差を1にする [3, 11]。
*   **正規化**：データを一定の範囲（0〜1）に収める [3, 11]。
*   **白色化**：**データ間の相関をなくし**、かつ分散を1にする [3, 11]。

### 7. 特徴量選択 vs 特徴抽出
*   **特徴量選択**：元のデータ列から必要なものを選び取る（Lassoやステップワイズ法など） [3]。
*   **特徴抽出**：元のデータ列を数式で組み合わせて、新しい変数を作る（PCA、t-SNEなど） [3, 5]。

### 8. 強化学習・Actor-Criticの基本
*   **基本構成**：状態（s）、行動（a）、報酬（r） [10, 14]。
*   **マルコフ性**：未来の状態は「現在の状態と行動」のみに依存する [10, 14]。
*   **価値と方策**：
    *   **価値関数**：将来の累積報酬の期待値 [10, 15]。
    *   **方策**：行動ルール（どの行動を取るか） [10, 15]。
    *   **Q値**：特定の状態で、特定の行動をとる価値 [10, 14]。
*   **Actor-Critic**：**Actor（行動選択）**と**Critic（価値評価）**を並行して学習させ、毎ステップで更新する [14]。

### 9. 評価指標：Precision vs Recall
*   **適合率（Precision）**：$TP / (TP + FP)$。陽性と判定した中での正解率。**偽陽性を防ぎたい**（スパム判定） [3]。
*   **再現率（Recall）**：$TP / (TP + FN)$。本物の陽性から見落とさない率。**偽陰性を防ぎたい**（がん検診） [3]。

### 10. 情報量基準：AIC vs BIC【資料外補足】
*   **目的**：モデルの適合度とパラメータ数のバランスを取り、最適なモデルを選ぶ基準。
*   **違い**：サンプルサイズが大きくなると、**BIC**のほうがパラメータ数に対するペナルティが厳しくなり、より単純なモデルを選びやすい。

### 11. 過学習 vs 未学習（トレードオフ）
*   **過学習**：表現力が過剰、特徴量が多すぎる、データが少なすぎる。訓練データにだけ強くなる [3]。
*   **未学習**：モデルの表現力が足りない、訓練すら不十分。訓練データでも正解率が低い [3]。
