# G検定 学習ツール 完成仕様書

最終更新：2026年8月20日

---

## 1. 3アプリの役割

| ツール | ファイル | 役割 | 使う場面 |
|---|---|---|---|
| 100問アプリ | `gkentei-100q.html` | 実力測定・演習・誤答復習 | まとまった学習時間 |
| 用語集 | `gkentei-yougo.html` | 調べる・比較する | 迷ったとき |
| 瞬殺トレーニング | `gkentei-flash.html` | 反射化 | すきま時間・試験直前 |

**設計思想**：ローカル437語で即決できるなら最速。足りないときだけWeb。2択の決定打がどうしても分からないときにAIへ進む。補助ツールが学習そのものを置き換えないよう、役割を明確に分離している。

---

## 2. gkentei-100q.html（282KB）

### データ

| 区分 | 問数 | ID |
|---|---|---|
| 既存問題 | 100 | 1〜100 |
| 実戦テスト 第1セット | 30 | P001〜P030 |
| 実戦テスト 第2セット | 30 | P031〜P060 |
| **合計** | **160** | |

既存100問は市販教材から**機械抽出**したもので、原文を一切改変していない（正本とのdiff 0件を確認済み）。実戦60問は既存100問の論点を応用形式で問い直すために新規作成した独立データ。

### 主な機能

**学習モード** … 10/20/50/100問、間違えた問題、未回答問題、苦手問題、技術分野、法律・倫理分野、シラバス別（55項目）

**採点方式** … 「1問ずつ採点」と「テストモード（全問終了後にまとめて採点）」を切替

**出題中の表示制御** … 問題タイトルとシラバスIDを非表示（タイトルが正解語そのものになっている問題があるため）。採点後に表示

**実戦テスト** … セット選択 → 出題数（10/20/30）→ 出題範囲（すべて／未出題だけ）。選択肢と出題順をシャッフルし、テスト中は正誤も解説も出さない

**誤答復習** … 一度でも間違えた問題を蓄積表示。フィルター8種（すべて／現在も不正解／過去に誤答あり／初見誤答のみ／第1／第2／技術／法倫）、並び順3種

### localStorage

```
gkentei_100q_v1       既存100問の履歴
gkentei_practical_v1  実戦問題の履歴（第1・第2セット共通）
```

実戦側は問題IDが一意なため、セットが増えてもmigration不要。

**初見履歴の3分離**

| フィールド | 記録タイミング |
|---|---|
| `firstSeenAt` | 問題を初めて**表示**した時点 |
| `firstAnsweredAt` | 初めて**解答**した時点 |
| `firstResult` | 初回の正誤。**以後上書きしない** |

表示だけで中断した場合は `firstSeenAt` のみ記録され、`firstAnsweredAt` は null。解き直して正解しても `firstResult` は変わらないため、初期の弱点が永続的に残る。

**時間計測**

- 総所要時間 … テスト開始から提出までの実経過時間
- 問題別回答時間 … その問題を表示していた時間の**累積**（行き来した分は合算）
- 採点後の見直し・解説閲覧は加算しない
- バックグラウンド時間は総所要・問題別の**両方に含まれる**（仕様として統一）

### 選択肢シャッフル

セッション開始時に問題ごとの並び替え表を生成して**セッション内で固定**。ユーザーの選択は必ず元の添字へ戻してから採点する。正解の表示位置は毎回算出。`answer` の数値は書き換えない。

### リセット

3系統に分離（既存100問／第1セット／第2セット）。片方をリセットしても他方は残る。

---

## 3. gkentei-yougo.html（224KB）

### データ構造（3層）

| レイヤー | 内容 | 語数 |
|---|---|---|
| CHAPTERS | 教科書（スッキリわかる第3版）の原文。**改変しない** | 395 |
| STUDY_META | CHAPTERS用語への試験向けメタ情報 | 342件 |
| SYLLABUS_EXTRA | 教科書に無いがシラバス第1.4版に明記された語 | 42 |
| **合計** | | **437語** |

`getTermMeta()` が3層を横断してメタ情報を解決するため、通常表示・検索・暗記モード・比較モードのすべてから同じデータを参照できる。

### 検索仕様

**1語検索** … 従来どおり `includes()` ベース。並びは章順

**複数語検索（2語以上）** … OR検索。「一致語数 → フィールド重み → 章順」で順位付け

```
フィールド重み： 用語名16 ＞ keywords8 ＞ short4 ＞ compare2 ＞ desc1
```

**検索中の表示** … 章見出し・節見出しを非表示にし、全カードを横断表示。**同一用語が複数章にある場合は最高スコアの1枚に集約**

**表記ゆれ吸収** … 複数語検索でのみ適用（5組）

```
画素↔ピクセル／個体↔インスタンス／誰↔話者／見逃し↔偽陰性／誤検知↔偽陽性
```

**コンパクト検索モード** … 検索中は `body.searching` により章チップ・表示切替・比較モード・ステータスバーを隠す。PC1280pxで**検索欄→第1結果18px**、ヘッダー高さ356px→89px

### 比較チップ10個

検索欄の下に常設（検索開始で自動的に隠れる）。

1. 勾配消失 vs 勾配爆発
2. L1 vs L2
3. 多クラス vs 多ラベル
4. セマンティック vs インスタンス
5. GAN vs VAE vs 拡散
6. 適合率 vs 再現率
7. 転移学習 vs 破滅的忘却
8. 匿名加工 vs 仮名加工
9. 請負 vs 準委任
10. 新規性 vs 進歩性

配列は「共通弱点 → 第2セット誤答 → 第1セット誤答 → 法務系」の優先度順。

### Web / AI 導線

検索欄と同じ行に配置。入力前は `aria-disabled="true"` で淡色表示。

| ボタン | 遷移先 |
|---|---|
| 🌐 Web | `https://www.google.com/search?q={G検定+入力}` |
| ✨ AI | 同上 ＋ `&udm=50`（Google AIモード） |
| 公式 | 同上 ＋ `site:` 指定（該当時のみ**最大1つ**） |

**`udm=50` は非公式パラメータ**のため `GOOGLE_AI_MODE_UDM` として1箇所に定数化。仕様変更時は1行で修正できる。無効化されても通常検索へフォールバックするだけでリンクは壊れない。

公式情報は明示キーワード表で判定し、複数該当時は一致数が最多の1機関のみ。同率なら表示しない。

```
個人情報保護委員会／文化庁／特許庁／経済産業省／総務省／公正取引委員会
```

### URLパラメータ

```
gkentei-yougo.html?q=適合率 再現率
```

その語で検索した状態で起動する。既存 `runSearch()` をそのまま呼ぶため、OR検索・ランキング・重複排除がすべて連動。

- 100文字で切り詰め
- 不正な値は通常起動へフォールバック
- **フォーカスは当てない**（スマホでキーボードが結果を隠さないため）

### 比較モード

25組。「話者識別 vs 音声認識」「知的財産の3制度」など、既存語と補完語が混在するグループを含む。

### localStorage

```
gkentei_yougo_status_v1   記憶状況（未／要確認／記憶済）
```

検索履歴は保存しない。

---

## 4. gkentei-flash.html（35KB）

### カードデータ設計

26論点。`items[].home` に所属を持たせ、**1つのデータから3用途を生成**する。

```js
{ id:"T01", type:"odd", cat:"最適化", weight:3,
  items:[{term:"AdaGrad", home:"最適化"}, ...],
  answer:3,
  key:"Boost が付くのはアンサンブル。他は最適化",
  look:"バギング ブースティング" }
```

| type | 出題形式 | 論点数 |
|---|---|---|
| odd | 仲間外れを選ぶ（items 4件） | 5 |
| pair | 用語の所属を答える（items 2件） | 13 |
| trio | 同上（items 3件） | 8 |

**内訳**：技術20論点／法務・倫理6論点。26論点のうち**17論点が実際の誤答由来**。

### 誤答肢の自動生成

`decoys`（手動指定）→ 同カテゴリの他カードの `home` → 全カードの `home` の順に補う。同カテゴリから選ぶため、**実際に混同しやすい選択肢**になる。全26カード×20回で「4択・正解1つ・重複なし」を検証済み。

### 速度判定（5区分）

```js
FAST_MS = 5000      // 5秒以内の正解＝定着候補
SLOW_MS = 10000     // 5〜10秒＝理解済み、10秒超＝要反射
STREAK_FIX = 2      // 5秒以内を2回連続で正解すると定着確認済み
SESSION_N = 20      // 1セッション20問
```

| 状態 | 条件 | 意味 |
|---|---|---|
| 弱点 | 直近が不正解 | 知識そのものが不足 |
| 要反射 | 未回答、または直近が10秒超の正解 | 最優先で反射化する |
| 理解済み | 直近が5〜10秒の正解 | 意味は分かるが反射化の途中 |
| 定着候補 | 直近が5秒以内の正解（連続1回） | あと1回で確定 |
| 定着確認済み | 5秒以内の正解が2回連続 | 反射化できている |

**5秒を超えた正解は `streak` が伸びない。** 正解でも12秒かかっていれば反射になっていないため。

判定は `lastResult`（直近の正誤）と `lastMs`（直近の所要時間）から算出する。

### 操作

**即時判定**（「回答する」ボタンなし）。反射を鍛えるのが目的なので、確定操作を挟まない。100問アプリとは意図的に逆の設計。

### 結果画面

正答率・5秒以内の件数・平均時間に加え、**速度別に2つの枠**で表示する。

- **要反射（10秒超で正解）** … 最優先で反射化すべき論点
- **理解済み（5〜10秒で正解）** … 意味は分かっているが反射化の途中

知ってはいるが反射になっていない論点が、優先度つきで可視化される。

### 用語集への導線

各カードから `gkentei-yougo.html?q={look}` へ別タブで遷移。全26論点でリンク先が0件にならないことを実測確認済み。

### A4瞬殺シート

同じ `CARDS` から生成するため二重管理にならない。**3ページ構成（10/10/6論点）**。

- ★＝仲間外れ、**太字**＝決定打
- 色を使わず白黒印刷に対応
- 起動時にシートへの掲載漏れを自己検査（実際にT08の漏れを検出した）

### localStorage

```
gkentei_flash_v1   {ok, ng, streak, last, bestMs, lastMs, lastResult}
```

`lastMs` と `lastResult` は5区分の状態判定に使う。

---

## 5. 共通仕様

### ファイル形式

**単一HTML・外部依存なし。** GitHub Pagesへそのまま配置できる。CDN・npm・APIキーを一切使わない。

### localStorageキー（4つ・完全分離）

```
gkentei_100q_v1
gkentei_practical_v1
gkentei_yougo_status_v1
gkentei_flash_v1
```

いずれかをリセットしても他は影響を受けない。

### 表示

PC 1280px・スマホ 390px／360px で横スクロールなし。タップ領域は44〜54px以上を確保。セーフエリア（ノッチ・ホームインジケータ）対応。

### 未実装（意図的に入れていないもの）

PWA／ダークモード／ログイン／クラウド同期／Web本文の取得・要約／AI API接続／APIキー保存／検索履歴の保存

---

## 6. 自動テスト

| 対象 | 項目数 |
|---|---|
| 100問アプリ | 333 |
| 用語集 | 421 |
| 瞬殺トレーニング | 70 |
| **合計** | **824** |

すべてjsdomによる実動作テスト。JSエラー0件。

---

## 7. データ整合性の保証

| 対象 | 検証方法 | 結果 |
|---|---|---|
| 既存100問 | 正本HTMLとの全断片突合 | 差分0件 |
| 実戦第1セット | 承認済みドラフトとの比較 | 差分0件 |
| 実戦第2セット | 同上 | 差分0件 |
| CHAPTERS | 元ファイルとのバイト比較 | 無変更 |
| STUDY_META | 同上 | 無変更 |

シラバスはJDLA公式「G検定 試験出題範囲（シラバス2024）第1.4版」（2026年5月11日）に準拠。技術37項目＋法律倫理18項目＝55項目。
