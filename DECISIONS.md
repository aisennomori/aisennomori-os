> Documentation Standard: Edition 1 / Version 2.0　|　Layer: OS Root　|　Status: Approved

# DECISIONS.md（OS Root）

このドキュメントは、**特定の1アプリに閉じない、愛泉の杜OS全体にまたがる設計判断**を記録する。

個別アプリ固有の判断は、各アプリのDOCS内の`DECISIONS.md`（例：`products/moon-tools/DOCS/MoonCard/03_Operation/DECISIONS.md`）に書く。ここに書くのは、複数アプリに影響する、あるいは複数アプリで再利用されるべき判断だけである。

> **執筆ルール**
> - 推測を書かない。実際に採用された設計判断だけを書く。
> - 採用されなかった案は記載しない。比較検討のログはこのファイルの目的ではない。
> - 1決定＝1エントリ。ID・カテゴリ・決定・理由・（あれば）将来の見直し条件、の構成で書く。
> - IDは `OS-DEC-XXX`（3桁連番）。個別アプリの`DEC-XXX`と混同しないよう、必ず`OS-`を付与する。カテゴリは `Data` / `UI` / `Architecture` / `Documentation` / `Performance` のいずれかを付与する。
> - **置き場所の判断基準**：影響範囲が1アプリに閉じるか、複数アプリにまたがるかで置き場所を決める。1アプリ→そのアプリのDECISIONS.md。複数アプリ→このファイル。

---

## OS-DEC-001：満月の瞬間は近似せず、k+0.5で直接計算する

**カテゴリ**：Architecture

**決定**：月相（New Moon / Full Moon）を扱うツールでは、満月は前後の新月の中間点で近似せず、Meeusの式に対して lunation number = k+0.5 を用いて直接計算する。

**理由**
- 月の公転は楕円軌道のため、新月〜満月の実際の日数は平均（約14.77日）から最大1日近くズレることがある（実例：1968年10月は14.03日、2026年7月は15.20日）
- 中間点近似では、このズレが大きい月ほど満月の「日付」自体がズレる（実例：1968年10月で1日分のズレを確認）
- New MoonとFull Moonは、Meeusの式において同じ補正項を共有しているため、`k+0.5`を使うだけで追加の実装コストなく正確な瞬間が得られる

**将来の見直し**：ハワイ月暦系ツール（MoonCard等）や、今後新設される月相関連ツールでも、この方式を踏襲する。

---

## OS-DEC-002：参照アプリとの表示一致より、計算根拠の正確性を優先する

**カテゴリ**：Architecture

**決定**：月相を扱うツールの数値（輝面比%等）は、他の月齢カレンダーアプリとの一致を目標にしない。天文学的な計算根拠（Meeusの式、正確な新月・満月の瞬間）を最優先とする。

**理由**
- 参照アプリの内部ロジックは非公開であり、一致させること自体を目的にすると根拠のない調整を重ねることになる
- 他アプリとの差異は、設計誤りの証拠ではなく、アルゴリズム差異の可能性として扱う
- 実際に検証した結果、数値差は「月の公転速度が一定ではない」という物理現象で説明できることを確認した（`DATA_AUDIT.md`のOS-AUDIT-001参照）

---

## OS-DEC-003：OS-root Documentationは、必要性が実際に発生してから作る

**カテゴリ**：Documentation

**決定**：`Documentation/`直下のOS-root文書（`APP_REGISTRY.md`・`DECISIONS.md`・`DATA_AUDIT.md`）は、将来を見越して先に体系を作らない。個別アプリのDOCSでは適切に保持できない知識（＝複数アプリにまたがる設計判断・監査）が実際に生まれた時点で初めて設ける。

**理由**
- 「良い分類を思いついたから追加する」ことと、「運用上の必要性が実際に発生したから追加する」ことは区別される。前者は先回り設計であり、後者だけが正当な追加理由になる
- 実際に、MoonAgeCalendarの開発で「月相計算」という複数アプリにまたがる設計判断が生まれたことが、OS-root Documentation新設の直接のきっかけになった（それ以前のFUTURE_IDEAS/SEEDS命名の議論は、この時点ではまだ実例ではなく予兆に留まっていた）
- カテゴリ（`VALIDATION.md`等）についても同じ基準を適用する。今は`DATA_AUDIT.md`内の「調査」セクションに留め、同種の記録が繰り返し必要になった実例が積み重なってから独立を検討する

**将来の見直し**：OS-root Documentationにさらにカテゴリを追加したくなった場合、必ず「これは実際に運用で繰り返し必要になったか、それとも思いつきか」を自問してから判断する。

---

## OS-DEC-004：Documentation Standard と APP_REGISTRY の責務を分離する

**カテゴリ**：Documentation

**決定**：`DOC_STANDARD.md`は、アプリの論理構造・文書構成・執筆ルールのみを定義する。論理アプリと実装・Documentation・関連資産の対応関係は、`APP_REGISTRY.md`を唯一の正本として管理する。OS-root Documentationには、OS全体の長期的な設計原則のみを記録する。

**理由**
- Documentation Standardが物理構造に依存すると、実装や資産の移動のたびに標準自体が古くなる
- 対応関係をAPP_REGISTRYに集約することで、物理構造変更時の更新箇所を一つに限定できる
- OS-root Documentationを長期的な設計原則に限定することで、個別アプリの運用履歴との混在を防ぐ

**将来の見直し**：APP_REGISTRYで実装・Documentation・関連資産の対応を管理できない実例が、複数アプリで継続的に発生した場合に見直す。

---

## OS-DEC-005：コンテンツ編集と品質保証の役割を分離する

**カテゴリ**：Documentation

**決定**：愛泉の杜OSにおけるコンテンツ制作では、編集、品質保証、最終承認の役割を分離する。コンテンツの正本は編集担当が管理し、品質保証担当は正本を変更せず、根拠を示した客観評価と参考提案を行う。最終的な採用判断はブランドオーナーが行う。

**理由**
- マナカード制作の実運用で、創作と品質保証を分離することで、ブランド資産の表現を守りながらデータ整合性・再利用性・保守性を確認できた
- 正本を変更しないQA運用により、提案・承認・反映の履歴を追跡可能にできる
- この役割分担は、カード以外のコンテンツ制作にも長期的に適用できる

**将来の見直し**：運用の中で、現在の一文書では管理できない独立した品質保証・版管理の実例が継続的に生じた場合に、文書分割を検討する。

---

## OS-DEC-006：戦略ロードマップと個別開発計画の責務を分離する

**カテゴリ**：Documentation

**決定**：OS全体の戦略管理と、個別開発計画は責務を分離し、それぞれ独立したロードマップで管理する。実装例として、`ROADMAP.md`は愛泉の杜OS全体の戦略・優先順位・完成条件・Statusを管理し、`SPRINTS/Roadmap.md`はSoul／BRAINの個別開発計画とSprint進捗を管理する。

また、Versionは成果物・システムの成熟度と到達した節目を表し、Statusは現在の制作・品質保証・リリース工程を表すものとして、独立して管理する。

**理由**
- OS全体の成熟度とSoul／BRAINの個別開発進捗は、対象・更新頻度・完成条件が異なる
- Version 1.0の意味が複数存在すると、進捗判断と対外的な説明が曖昧になる
- Content Complete、QA Complete、Releasedを区別することで、コンテンツ完成と正式公開を混同しない

**将来の見直し**：個別開発計画が増え、現行のロードマップ構成で責務を表現できなくなった場合に見直す。

---

## OS-DEC-007：Codex原本更新時は、版管理担当（Aloha）へ通知する（Original Update Notification Rule）

**カテゴリ**：Documentation

**決定**：CodexがCurrent（原本）を直接更新した場合、版管理担当（Aloha）へ、更新ファイル・Edition/Version・更新理由（1〜2行）・Currentへ反映済みであることを共有する。

**理由**
- 原本（Current）がCodexにより直接更新された際、その更新情報が版管理担当へ共有されないことで、最新版の認識差異が複数のOS-root文書（`DOC_STANDARD.md`・`DECISIONS.md`）で発生したことを確認した
- これは個別のミスではなく、情報共有経路が存在しなかったことによる構造的な課題である
- 今後はOriginal Update Notification Ruleを標準運用とし、原本更新時には版管理担当へ更新内容を共有する

**将来の見直し**：通知の運用が定着し、Aloha側での確認漏れが継続的に発生しなくなった場合、通知の粒度（頻度・形式）を見直してよい。
